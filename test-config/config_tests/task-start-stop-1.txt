task a = start_stop
{
    start = command
    {
        cmd = "/bin/ls",
        working_directory = "/"
    },

    stop = command
    {
        cmd = "/bin/ls",
        working_directory = "/"
    }
};
 
